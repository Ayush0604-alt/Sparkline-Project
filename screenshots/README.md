# Screenshots

This folder is a placeholder. Add the following screenshots after running the
app locally (see README.md → "Local Setup"):

1. **dashboard.png** — Full dashboard view showing Dashboard Stats + Crane
   Overview KPI cards (all 3 cranes visible, orange industrial theme).
2. **chart.png** — Temperature Trend section with a crane selected from the
   dropdown, showing the line chart with the 80°C alert threshold reference
   line visible.
3. **alerts.png** — Alerts List section showing several alerts, with at
   least one "Critical" highlighted row visible (temp ≥ 85°C).

Optional extras worth capturing for the interview demo:
- Dark mode toggled on
- The search/filter box in Alerts List narrowing results to one crane
- A terminal screenshot of `npm run seed` output showing alert log lines
